services:
  web:
    image: ghcr.io/lennarddevries/joovi-web:dev
    container_name: joovi-web-dev
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=development
      - API_URL=http://server:8000
    depends_on:
      - server
    networks:
      - joovi-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4321"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  server:
    image: ghcr.io/lennarddevries/joovi-server:dev
    container_name: joovi-server-dev
    ports:
      - "8000:8000"
    environment:
      - PYTHON_ENV=development
    networks:
      - joovi-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  joovi-network:
    driver: bridge
